---
# Add manual deposits and ledger models
# Run server (it will sync DB with new models)
# Example: approve a deposit
# POST /api/manual-deposits/admin/:id/approve with JSON { "matched_bank_txn_id": "UTR123" }
---
